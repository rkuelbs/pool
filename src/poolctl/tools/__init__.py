"""
Operational CLI tools for on-device diagnostics and bringup.

These modules are meant to be run with ``python -m poolctl.tools.<name>`` on
the Raspberry Pi when checking RS485 wiring, Modbus addresses, baud rates, or
analog input mode before the full controller is left running.
"""
